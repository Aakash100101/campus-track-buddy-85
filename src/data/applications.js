// The five placement statuses used across CampusTrack.
// Application data itself now lives in the database (see applicationService.js).

export const STATUSES = ["Applied", "Assessment", "Interview", "Selected", "Rejected"];
